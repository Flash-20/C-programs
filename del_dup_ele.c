#include<stdio.h>
int main() {
    int array[50], i, size;
    scanf("%d", &size);
    for(i=0; i<size; i++)
        scanf("%d", &array[i]);
    int array1[50], j;
    for(i=0,j=0; i<size; i++,j++)
        array1[i]=array[i]
}